{% extends 'demo/layout.html.twig' %}

{% block title %}Blog Index{% endblock %}

{% block page_contents %}
layout
    {# {% for article in articles %}
        <h2>{{ article.title }}</h2>
        <p>{{ article.body }}</p>
    {% endfor %} #}
{% endblock %}