{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {# <h1> {{ userFirstName }}! ✅</h1> #}

    {# <p>{{userNotification['hello']}}</p> #}
    {# <p>{{ userFirstName }} added this comment on {{ comment.publishedAt|date }}</p> #}
</div>

{# <a href="{{ path('blog_index') }}">Homepage</a> #}
        {# <a href="{{ path('blog_post', {slug: post.slug}) }}">{{ post.title }}</a> #}

{# ... #}

{# {% for post in blog_posts %}
    <h1>
        <a href="{{ path('blog_post', {slug: post.slug}) }}">{{ post.title }}</a>
    </h1>

    <p>{{ post.excerpt }}</p>
{% endfor %} #}
{# <div>
    <div> image template</div>
<img src="{{ absolute_url(asset('/home/chintan/web/projects/Learning/Kishan/symfony/demoSymfony/public/images/after-query.png')) }}" alt="Symfony!"/>

{# the CSS file lives at "public/css/blog.css" #}
{# <link href="{{ asset('/home/chintan/web/projects/Learning/Kishan/symfony/demoSymfony/public/css/style.css') }}" rel="stylesheet"/> #}

{# the JS file lives at "public/bundles/acme/js/loader.js" #}
{# <script src="{{ asset('/home/chintan/web/projects/Learning/Kishan/symfony/demoSymfony/public/js/demo.js') }}"></script> #}
{# </div> #} 

{# <p>Username: {{ app.user.username ?? 'Anonymous user' }}</p>
{% if app.debug %}
    <p>Request method: {{ app.request.method }}</p>
    <p>Application Environment: {{ app.environment }}</p>
{% endif %} #}
{# <h1> {{category}}</h1>
<h1> {{promotions['h']}}</h1> #}

{# templates/emails/signup.html.twig #}
{# <h1>Welcome {{ email.toName }}!</h1> #}

{# <p>
    You signed up as {{ username }} the following email:
</p>
<p><code>{{ email.to[0].address }}</code></p>

<p>
    <a href="#">Click here to activate your account</a>
    (this link is valid until {{ expiration_date|date('F jS') }})
</p> #}





{# templates/article/recent_list.html.twig #}
{# the contents of this variable are sent to the Web Debug Toolbar
   instead of dumping them inside the page contents #}
{# {% dump articles) %}

{% for article in articles %}
    {{article}}
    {# the contents of this variable are dumped inside the page contents
       and they are visible on the web page #}
    {# {{ dump(article) }}

    <a href="/article/{{ article.slug }}">
        {{ article.title }}
    </a>
{% endfor %} #} 
{# {{include('blog/index.html.twig')}} #}
{# templates/blog/index.html.twig #}

{# ... #}
{# {{ include('blog/index.html.twig', {user: blog_post.author}) }} #}
{# templates/blog/_recent_articles.html.twig #}
{# {% for article in articles %}
    <a href="{{ path('dump', {max: article.max}) }}">
        {{ article.title }}
    </a>
{% endfor %} #}
{# {% dump articles %/} #}

{# {% for article in articles %}
    {# the contents of this variable are dumped inside the page contents
       and they are visible on the web page #}
    {# {{ dump(article) }}

    <a href="/article/{{ article }}">
        {{ article }}
    </a> #} 

{# {% endfor %} #}
{# templates/emails/signup.html.twig #}
{# <h1>Welcome {{ emailto[0].address }}!</h1>

<p>
    You signed up as {{ username }} the following email:
</p>
<p><code>{{ email.to[0].address }}</code></p>

<p>
    <a href="#">Click here to activate your account</a>
    (this link is valid until {{ expiration_date|date('F jS') }})
</p> #}
{# templates/blog/index.html.twig #}

{# ... #}
{# {{ include('blog/index.html.twig', {user:new_post.html.twig}) }} #}
{# templates/blog/_recent_articles.html.twig #}
{% for article in articles %}
    <a href="{{ path('blog_index', {max:max}) }}">
        {{ article }}
    </a>
{% endfor %}    
{% endblock %}  